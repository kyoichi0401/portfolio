<h1>イベント一覧</h1>
<ul>
<% @conferences&.each{|conf| %>
  <li><%= link_to conf.name, conference_path(conf) %> ( <%= conf.start_date %>
  <% if conf.duration > 1 then %>
    to <%= conf.start_date + conf.duration-1 %>
  <% end %>)</li>
  <% } %>
  </ul>

